export interface StepData {
    imgSrc: string,
    title: string,
    description: string
}

export interface VisibleConfig {
    image: boolean,
    title: boolean,
    description: boolean,
    button: boolean,
}